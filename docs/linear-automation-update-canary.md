# Linear Automation Update-Qualification Canary

> Temporary docs-only canary. Do not merge, auto-merge, or deploy.

This file is a temporary, docs-only canary that confirms a previously
unqualified Linear issue is picked up exactly once after an update makes
it fully eligible for the Centaur Linear readiness policy.

It is intentionally minimal: no runtime configuration, code, or
credentials are touched, and no other files are modified.
